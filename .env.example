# Kopieren nach .env und Werte anpassen:  cp .env.example .env

# Deine Domain (A-Record muss auf die Server-IP zeigen)
DOMAIN=fotos.example.ch

# Geheimer Schlüssel für die Moderations-Seite (/mod).
# Erzeugen mit:  openssl rand -hex 16
MOD_KEY=bitte-aendern

TZ=Europe/Zurich
